name: Deployment

on:
  push:
    branches:
      - main

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: deploy
       uses: actions/checkout@v3
        # ...deployment-specific steps
